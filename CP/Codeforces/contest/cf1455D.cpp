#define encoding_898                 \
    ios_base::sync_with_stdio(false);\
    cin.tie(NULL);                   \
    cout.tie(NULL);                  \
    cerr.tie(NULL);
#include<bits/stdc++.h>
using namespace std;
int main(void){
    encoding_898;
    int t;
    cin>>t;
    while(t--){
        int i,index,n,x,a,b,total=0;
        cin>>n>>x;
        for(i=0;i<n;i++){
            cin>>a;
            if(a<x)
                index=i;
            if(b>a){
                if()
            }
            b=a;
        }
    }
}